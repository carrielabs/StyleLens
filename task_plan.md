# StyleLens 工作区拆分看板

## 目标

把首页拆成两个清晰工作区：

1. 提取视觉风格：只处理 URL / 图片风格提取，只显示提取历史。
2. 生成页面与看板：只处理模板库、模板预览、文本/文件生成 HTML，只显示生成历史。

## 可视化看板

| 待处理 | 进行中 | 已完成 |
| --- | --- | --- |
|  |  | 开工检查：`main...origin/main`，工作区干净 |
|  |  | 确认只能在主工作区 `main` 开发 |
|  |  | 确认现有真实模板和生成 API 可复用 |
|  |  | TDD RED 测试已建立 |
|  |  | 实现左侧两个工作区菜单 |
|  |  | 拆出生成页面与看板工作区 |
|  |  | 实现全屏模板预览 |
|  |  | 实现右侧配置抽屉 |
|  |  | 实现生成记录本地历史 |
|  |  | 改造生成结果页顶部操作条 |
|  |  | 跑硬测试：单测、类型检查、构建 |
|  |  | 提交并准备 push 到 `origin/main` |

## 任务清单

- [x] 确认当前目录、分支、基线、工作区状态。
- [x] 用测试锁定工作区切换、模板预览、抽屉生成、生成历史。
- [x] 拆分提取工作区和 Publisher 工作区。
- [x] 左侧菜单支持两个工作区，并按工作区展示对应历史。
- [x] Publisher 工作区展示真实模板库和分类。
- [x] 模板卡片支持全屏预览和使用模板。
- [x] 右侧抽屉支持文本、`.md`、`.txt` 和数据文件生成。
- [x] 生成结果页保留 iframe 下载逻辑，改为悬浮操作条。
- [x] 硬测试通过。
- [x] 提交并准备 push 到 `origin/main`。

## 修改边界

- 不复制 Gemini HTML 代码。
- 不使用 Tailwind CDN、`onclick`、`innerHTML` 原型写法。
- 不改核心生成 API，优先复用现有 `/api/generate` 和 `/api/generate-dashboard-data`。
- 不启动浏览器、不截图、不跑视觉测试。

## 错误记录

| 时间 | 错误 | 处理 |
| --- | --- | --- |
| 2026-07-12 | `session-catchup.py` 长时间无输出 | 已中断，使用 `git status` 和开工检查作为准入依据 |
