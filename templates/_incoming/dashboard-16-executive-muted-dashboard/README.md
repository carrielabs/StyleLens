# dashboard-16-executive-muted-dashboard

类型：数据报告模板

编号：16

风格：Executive Muted / 企业级数据分析看板

适合：月度或季度经营复盘、管理层大盘、财务与运营深度分析、B2B 多维指标监控。

说明：本模板来自用户提供的 `code_artifact (2).html` 和同内容粘贴附件。入库版保留 Gemini 原始视觉方向，按仓库标准整理为独立模板目录，并补齐 `template.json`、设计规范、槽位说明、预览元数据和索引注册。

交付检查：

- HTML：20 个可编辑节点均包含 `data-editable="true"`、`contenteditable="true"` 和 `data-slot`。
- 图表：10 个 ECharts 图表容器，均符合 `chart-*` 命名并带 `data-chart-type`。
- 模块：4 个 `data-section`，分别为 `key-takeaway`、`core-finding-1`、`core-finding-2`、`core-finding-3`。
- 原始文件：已归档到 `originals/source-template.html` 和 `originals/pasted-text.html`。

当前限制：未生成 `preview.png`。本轮按低性能模式执行，未打开浏览器、未截图、未启动 dev server。
