# dashboard-15-consulting-data-report

类型：数据报告模板

编号：15

风格：Consulting Data Report / 咨询级数据报告模板

适合：管理层汇报、商业数据复盘、市场趋势分析、竞争格局分析、战略行动方案。

说明：本模板来自用户提供的 `code_artifact (25).html` 和同内容粘贴附件。入库版保留 Gemini 原始视觉方向，补齐仓库统一的 `data-slot`、`contenteditable`、`template.json`、槽位说明和索引注册。

交付检查：

- HTML：77 个可编辑节点均包含 `data-editable="true"`、`contenteditable="true"` 和 `data-slot`。
- 图表：8 个 Chart.js 图表，图表数据已集中到模板底部 `const chartData`。
- 模块：11 个 `data-section`，包括 `report-header` 和 10 个滚动报告区块。
- 原始文件：已归档到 `originals/source-template.html` 和 `originals/pasted-text.html`。

当前限制：未生成 `preview.png`。本轮按低性能模式执行，未打开浏览器、未截图、未启动 dev server。
